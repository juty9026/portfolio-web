/** @jsxImportSource @emotion/react */
import { divider } from './Divider.emotion';

type DividerProps = {};
const Divider = ({}: DividerProps) => {
  return <div css={divider} />;
};

export default Divider;
