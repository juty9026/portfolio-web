import { css } from '@emotion/react';

const container = css`
  display: flex;
  flex-direction: row;
`;

const section = css`
  flex: 1;
  font-size: 1.5rem;
`;

export default { container, section };
